import openai from 'openai';\n\nexport async function useChatBotAPI(accessToken, message) {\n  openai.apiKey = accessToken;\n\n  try {\n    const response = await openai.Completion.create({\n      engine: 'text-davinci-codex',\n      prompt: message,\n      max_tokens: 150,\n      n: 1,\n      stop: null,\n    });\n\n    const output = response.choices[0].text.trim();\n    return output;\n  } catch (error) {\n    console.error('Error sending the message to the API:', error);\n    return 'Error: Could not send the message to the API';\n  }\n}