import React, { useState } from 'react';\nimport { useRouter } from 'next/router';\nimport Layout from '../../components/Layout';\nimport { supabase } from '../../utils/supabaseClient';\n\nconst Settings = () => {\n  const [apiKey, setApiKey] = useState('');\n  const router = useRouter();\n\n  const saveApiKey = async (event) => {\n    event.preventDefault();\n    //TODO: Save the API key to user settings in Supabase\n    setApiKey('');\n    router.push('/chat');\n  };\n\n  return (\n    <Layout>\n      <div className='flex flex-col h-full'>\n        <h1 className='text-xl mb-4'>Settings</h1>\n        <form onSubmit={saveApiKey} className='mt-4'>\n          <label htmlFor='api_key' className='mr-2'>OpenAI API Key:</label>\n          <input\n            type='text'\n            id='api_key'\n            value={apiKey}\n            onChange={(e) => setApiKey(e.target.value)}\n            className='w-full px-4 py-2 rounded-lg shadow-md'\n          />\n          <button type='submit' className='mt-4 px-4 py-2 bg-green-500 text-white rounded-lg shadow-md'>Save</button>\n        </form>\n      </div>\n    </Layout>\n  );\n};\n\nexport default Settings;